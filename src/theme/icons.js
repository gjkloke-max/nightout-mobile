/**
 * Icon sizing and color conventions — Lucide React Native.
 * Use with shared Icon component for consistent, premium iconography.
 *
 * Sizes:
 *   nav     — Bottom tab bar (24)
 *   header  — Top bar / header actions (22)
 *   button  — Primary action buttons (18–20)
 *   card    — Card metadata, list items (18)
 *   inline  — Inline with text (16)
 *   xs      — Small badges, chips (14)
 */

export const iconSizes = {
  nav: 24,
  header: 22,
  button: 20,
  card: 18,
  inline: 16,
  xs: 14,
}

export const iconStrokeWidth = {
  default: 2,
  light: 1.5,
  bold: 2.5,
}
