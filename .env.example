# NightOut Mobile — Environment Variables
# Copy to .env and fill in values from the main NightOut web app.

# Supabase (same as web app)
EXPO_PUBLIC_SUPABASE_URL=your_supabase_project_url
EXPO_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key

# Search API — for concierge/venue search
# Local dev: use your machine's LAN IP (e.g. http://192.168.1.100:3001)
# Production: your deployed API URL
EXPO_PUBLIC_SEARCH_API_URL=http://localhost:3001
